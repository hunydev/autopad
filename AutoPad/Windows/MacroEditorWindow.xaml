<Window x:Class="AutoPad.Windows.MacroEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AutoPad - Macro Editor"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        Width="700"
        Height="560"
        MinWidth="550"
        MinHeight="450"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#106EBE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 매크로 이름 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="NameLabel" Text="Name" Foreground="#E0E0E0" FontSize="13" Margin="0,0,0,6"/>
            <TextBox x:Name="NameTextBox"
                     Background="#2D2D30" Foreground="#E0E0E0"
                     BorderBrush="#3F3F46" BorderThickness="1"
                     Padding="10,8" FontSize="13"
                     CaretBrush="White"/>
        </StackPanel>

        <!-- 스크립트 라벨 -->
        <TextBlock Grid.Row="1" x:Name="ScriptLabel"
                   Text="Script (JavaScript)"
                   Foreground="#E0E0E0" FontSize="13" Margin="0,0,0,6"/>

        <!-- 스크립트 편집기 -->
        <TextBox Grid.Row="2" x:Name="ScriptTextBox"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Background="#1E1E1E"
                 Foreground="#D4D4D4"
                 BorderBrush="#3F3F46"
                 BorderThickness="1"
                 Padding="12"
                 FontFamily="Consolas"
                 FontSize="13"
                 CaretBrush="White"
                 SelectionBrush="#0078D4"/>

        <!-- 테스트 영역 -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock x:Name="TestInputLabel" Text="Test Input" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,4"/>
                <TextBox x:Name="TestInputTextBox"
                         AcceptsReturn="True"
                         Height="80"
                         VerticalScrollBarVisibility="Auto"
                         Background="#2D2D30" Foreground="#E0E0E0"
                         BorderBrush="#3F3F46" BorderThickness="1"
                         Padding="8" FontFamily="Consolas" FontSize="12"
                         CaretBrush="White"
                         Text="Hello World  123"/>
            </StackPanel>

            <Button Grid.Column="1" x:Name="TestRunButton"
                    Content="&#xE768;" FontFamily="Segoe Fluent Icons"
                    Click="TestRunButton_Click"
                    Background="#3C3C3C" Foreground="#E0E0E0" BorderThickness="0"
                    Width="36" Height="36" Cursor="Hand" FontSize="16"
                    VerticalAlignment="Center" Margin="8,16,8,0"
                    ToolTip="Run Test">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#505050"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Grid.Column="2">
                <TextBlock x:Name="TestOutputLabel" Text="Output" Foreground="#AAAAAA" FontSize="12" Margin="0,0,0,4"/>
                <TextBox x:Name="TestOutputTextBox"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         Height="80"
                         VerticalScrollBarVisibility="Auto"
                         Background="#252526" Foreground="#4EC9B0"
                         BorderBrush="#3F3F46" BorderThickness="1"
                         Padding="8" FontFamily="Consolas" FontSize="12"/>
            </StackPanel>
        </Grid>

        <!-- 에러 메시지 -->
        <TextBlock Grid.Row="4" x:Name="ErrorText"
                   Foreground="#F44747" FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0"/>

        <!-- 버튼 -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="Cancel"
                    Style="{StaticResource SecondaryButton}"
                    Click="CancelButton_Click"
                    Margin="0,0,8,0"/>
            <Button x:Name="SaveMacroButton" Content="Save"
                    Style="{StaticResource ActionButton}"
                    Click="SaveMacroButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
